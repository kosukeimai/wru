# Sample .travis.yml for R projects

language: r
cache: packages
sudo: required

r_build_args: --resave-data

r_binary_packages: 
  - piggyback
  - PL94171
  - Rcpp
  - RcppArmadillo
  
notifications:
  email:
    on_success: change
    on_failure: change

addons:
  apt:
    packages:
      - libgit2-dev